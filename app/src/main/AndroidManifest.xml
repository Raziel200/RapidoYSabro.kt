


<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rapido_y_Sabroso"
        tools:targetApi="31">
        <activity
            android:name=".activities.DetailedDailyMealActivity"
            android:exported="false" />
        <activity
            android:name=".activities.Menu_principal"
            android:exported="false"
            android:label="@string/title_activity_menu_principal"
            android:theme="@style/Theme.Rapido_y_Sabroso.NoActionBar" />
        <activity
            android:name=".activities.Registrar_usuario"
            android:exported="false" />
        <activity
            android:name=".activities.iniciar_secion"
            android:exported="false" />

        <activity
            android:name=".PrimerPantallaActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.ConfirmationActivity"
            android:exported="true" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
